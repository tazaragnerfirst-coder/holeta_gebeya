# Copy to frontend/.env for local dev, and set the same key
# in your hosting build settings (or a .env.production file)
# once the Render backend is deployed.
VITE_BACKEND_URL=https://your-render-service.onrender.com
