[1mdiff --git a/backend/server/index.js b/backend/server/index.js[m
[1mindex 8c43e7e..43052eb 100644[m
[1m--- a/backend/server/index.js[m
[1m+++ b/backend/server/index.js[m
[36m@@ -80,6 +80,56 @@[m [mapp.post('/telegramAuth', async (req, res) => {[m
   }[m
 });[m
 [m
[32m+[m[32m// Called by the client right after a chat message is written to[m
[32m+[m[32m// Firestore. Looks up the recipient's Telegram ID (stored on their[m
[32m+[m[32m// users/{uid} doc) and pings them via the Bot API — the client SDK[m
[32m+[m[32m// can't call Telegram directly (no bot token there), and we don't[m
[32m+[m[32m// have Cloud Functions/Firestore triggers on the free Spark plan.[m
[32m+[m[32mapp.post('/notifyNewMessage', async (req, res) => {[m
[32m+[m[32m  try {[m
[32m+[m[32m    if (!BOT_TOKEN) return res.status(500).json({ error: 'Bot token not configured on the server.' });[m
[32m+[m[32m    const { recipientUid, senderName, listingTitle, text, chatId } = req.body || {};[m
[32m+[m[32m    if (!recipientUid || !text) return res.status(400).json({ error: 'recipientUid and text are required.' });[m
[32m+[m
[32m+[m[32m    // "support" is a virtual participant (no Firebase Auth user, no[m
[32m+[m[32m    // Telegram ID to notify) — nothing to send.[m
[32m+[m[32m    if (recipientUid === 'support') return res.json({ ok: true, skipped: 'support' });[m
[32m+[m
[32m+[m[32m    const userSnap = await db.collection('users').doc(recipientUid).get();[m
[32m+[m[32m    const telegramId = userSnap.exists ? userSnap.data().telegramId : null;[m
[32m+[m[32m    if (!telegramId) return res.json({ ok: true, skipped: 'no telegramId on file' });[m
[32m+[m
[32m+[m[32m    const preview = text.length > 120 ? `${text.slice(0, 117)}...` : text;[m
[32m+[m[32m    const messageText = `💬 New message from ${senderName || 'someone'}`[m
[32m+[m[32m      + (listingTitle ? `\nAbout: "${listingTitle}"` : '')[m
[32m+[m[32m      + `\n\n${preview}`;[m
[32m+[m
[32m+[m[32m    const webAppUrl = process.env.MINI_APP_URL[m
[32m+[m[32m      ? `${process.env.MINI_APP_URL}#/chat/${chatId}`[m
[32m+[m[32m      : undefined;[m
[32m+[m
[32m+[m[32m    await fetch(`https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`, {[m
[32m+[m[32m      method: 'POST',[m
[32m+[m[32m      headers: { 'Content-Type': 'application/json' },[m
[32m+[m[32m      body: JSON.stringify({[m
[32m+[m[32m        chat_id: telegramId,[m
[32m+[m[32m        text: messageText,[m
[32m+[m[32m        ...(webAppUrl && chatId ? {[m
[32m+[m[32m          reply_markup: {[m
[32m+[m[32m            inline_keyboard: [[{ text: 'Open chat', web_app: { url: webAppUrl } }]],[m
[32m+[m[32m          },[m
[32m+[m[32m        } : {}),[m
[32m+[m[32m      }),[m
[32m+[m[32m    });[m
[32m+[m
[32m+[m[32m    res.json({ ok: true });[m
[32m+[m[32m  } catch (err) {[m
[32m+[m[32m    console.error('notifyNewMessage failed:', err);[m
[32m+[m[32m    // Never fail the chat send over a notification hiccup.[m
[32m+[m[32m    res.json({ ok: false });[m
[32m+[m[32m  }[m
[32m+[m[32m});[m
[32m+[m
 app.post('/incrementListingView', async (req, res) => {[m
   try {[m
     const id = req.body.listingId;[m
[1mdiff --git a/firebase.json b/firebase.json[m
[1mindex 1fd3569..524030b 100644[m
[1m--- a/firebase.json[m
[1m+++ b/firebase.json[m
[36m@@ -6,6 +6,20 @@[m
   "hosting": {[m
     "public": "frontend/dist",[m
     "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],[m
[31m-    "rewrites": [{ "source": "**", "destination": "/index.html" }][m
[32m+[m[32m    "rewrites": [{ "source": "**", "destination": "/index.html" }],[m
[32m+[m[32m    "headers": [[m
[32m+[m[32m      {[m
[32m+[m[32m        "source": "index.html",[m
[32m+[m[32m        "headers": [[m
[32m+[m[32m          { "key": "Cache-Control", "value": "no-cache, no-store, must-revalidate" }[m
[32m+[m[32m        ][m
[32m+[m[32m      },[m
[32m+[m[32m      {[m
[32m+[m[32m        "source": "**/*.@(js|css)",[m
[32m+[m[32m        "headers": [[m
[32m+[m[32m          { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }[m
[32m+[m[32m        ][m
[32m+[m[32m      }[m
[32m+[m[32m    ][m
   }[m
 }[m
[1mdiff --git a/frontend/src/App.jsx b/frontend/src/App.jsx[m
[1mindex 6403827..3fbac48 100644[m
[1m--- a/frontend/src/App.jsx[m
[1m+++ b/frontend/src/App.jsx[m
[36m@@ -31,7 +31,7 @@[m [mexport default function App() {[m
 function BottomNav() {[m
   const item = (to, label, icon) => ([m
     <NavLink to={to} className={({ isActive }) => `nav-item ${isActive ? 'active' : ''}`} end={to === '/'}>[m
[31m-      <Icon name={icon} size={20} />[m
[32m+[m[32m      <Icon name={icon} size={24} />[m
       <span>{label}</span>[m
     </NavLink>[m
   );[m
[36m@@ -39,7 +39,7 @@[m [mfunction BottomNav() {[m
     <nav className="bottom-nav">[m
       {item('/', 'Home', 'home')}[m
       {item('/chat', 'Chat', 'chat')}[m
[31m-      <NavLink to="/post" className="nav-fab"><Icon name="plus" size={22} /></NavLink>[m
[32m+[m[32m      <NavLink to="/post" className="nav-fab"><Icon name="plus" size={24} /></NavLink>[m
       {item('/dashboard', 'Dashboard', 'briefcase')}[m
       {item('/profile', 'Profile', 'user')}[m
     </nav>[m
[1mdiff --git a/frontend/src/components/Icon.jsx b/frontend/src/components/Icon.jsx[m
[1mindex 35e5a54..b81216f 100644[m
[1m--- a/frontend/src/components/Icon.jsx[m
[1m+++ b/frontend/src/components/Icon.jsx[m
[36m@@ -30,6 +30,10 @@[m [mconst PATHS = {[m
   car: '<path d="M4 16V12l2-5h12l2 5v4"/><path d="M4 16h16"/><circle cx="7.5" cy="17.5" r="1.6"/><circle cx="16.5" cy="17.5" r="1.6"/>',[m
   image: '<rect x="3" y="4.5" width="18" height="15" rx="2.5"/><circle cx="8.5" cy="10" r="1.6"/><path d="M4 16.5l5-4.5 3.5 3 3-2.5 4.5 4"/>',[m
   x: '<path d="M6 6l12 12M18 6L6 18"/>',[m
[32m+[m[32m  sliders: '<path d="M4 6h10M18 6h2M4 12h4M12 12h8M4 18h13M21 18h0"/><circle cx="16" cy="6" r="2.2"/><circle cx="8" cy="12" r="2.2"/><circle cx="17" cy="18" r="2.2"/>',[m
[32m+[m[32m  xCircle: '<circle cx="12" cy="12" r="9"/><path d="M9.5 9.5l5 5M14.5 9.5l-5 5"/>',[m
[32m+[m[32m  history: '<path d="M3 12a9 9 0 109-9 9.7 9.7 0 00-7 3"/><path d="M3 4v5h5"/><path d="M12 8v4l3 2"/>',[m
[32m+[m[32m  frown: '<circle cx="12" cy="12" r="9"/><path d="M8.5 15.5c1-1.2 2.2-1.8 3.5-1.8s2.5.6 3.5 1.8"/><circle cx="9" cy="9.5" r=".5" fill="currentColor" stroke="none"/><circle cx="15" cy="9.5" r=".5" fill="currentColor" stroke="none"/>',[m
 };[m
 [m
 export default function Icon({ name, size = 20, className = '', style, ...rest }) {[m
[1mdiff --git a/frontend/src/lib/firebase.js b/frontend/src/lib/firebase.js[m
[1mindex f3ae5bf..6e7a2e5 100644[m
[1m--- a/frontend/src/lib/firebase.js[m
[1m+++ b/frontend/src/lib/firebase.js[m
[36m@@ -20,6 +20,7 @@[m [mconst firebaseConfig = {[m
 // backend/server/index.js. Set VITE_BACKEND_URL in a .env file[m
 // (frontend/.env) or in Vite's build-time env vars.[m
 const BACKEND_URL = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3000';[m
[32m+[m[32mexport { BACKEND_URL };[m
 [m
 export const app = initializeApp(firebaseConfig);[m
 export const db = getFirestore(app);[m
[36m@@ -61,3 +62,13 @@[m [mexport function ensureLoggedIn() {[m
 [m
   return loginPromise;[m
 }[m
[32m+[m
[32m+[m[32m// Best-effort — never blocks or breaks message sending if it fails[m
[32m+[m[32m// (e.g. Render free-tier cold start). Fire-and-forget from callers.[m
[32m+[m[32mexport function notifyNewMessage({ recipientUid, senderName, listingTitle, text, chatId }) {[m
[32m+[m[32m  return fetch(`${BACKEND_URL}/notifyNewMessage`, {[m
[32m+[m[32m    method: 'POST',[m
[32m+[m[32m    headers: { 'Content-Type': 'application/json' },[m
[32m+[m[32m    body: JSON.stringify({ recipientUid, senderName, listingTitle, text, chatId }),[m
[32m+[m[32m  }).catch((err) => console.error('notifyNewMessage failed:', err));[m
[32m+[m[32m}[m
[1mdiff --git a/frontend/src/pages/ChatList.jsx b/frontend/src/pages/ChatList.jsx[m
[1mindex 076ad86..b438dfc 100644[m
[1m--- a/frontend/src/pages/ChatList.jsx[m
[1m+++ b/frontend/src/pages/ChatList.jsx[m
[36m@@ -2,14 +2,35 @@[m [mimport React, { useEffect, useState } from 'react';[m
 import { Link } from 'react-router-dom';[m
 import { collection, query, where, orderBy, onSnapshot } from 'firebase/firestore';[m
 import { db, ensureLoggedIn } from '../lib/firebase';[m
[32m+[m[32mimport { SUPPORT_NAME } from '../lib/constants';[m
 import Icon from '../components/Icon.jsx';[m
 [m
[32m+[m[32mfunction formatTime(ts) {[m
[32m+[m[32m  if (!ts?.toDate) return '';[m
[32m+[m[32m  const d = ts.toDate();[m
[32m+[m[32m  const now = new Date();[m
[32m+[m[32m  const sameDay = d.toDateString() === now.toDateString();[m
[32m+[m[32m  if (sameDay) return d.toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });[m
[32m+[m[32m  const yesterday = new Date(now); yesterday.setDate(now.getDate() - 1);[m
[32m+[m[32m  if (d.toDateString() === yesterday.toDateString()) return 'Yesterday';[m
[32m+[m[32m  return d.toLocaleDateString([], { month: 'short', day: 'numeric' });[m
[32m+[m[32m}[m
[32m+[m
[32m+[m[32mfunction isUnread(chat, uid) {[m
[32m+[m[32m  if (!chat.lastMessageAt || chat.lastSenderId === uid) return false;[m
[32m+[m[32m  const readAt = chat.lastReadAt?.[uid];[m
[32m+[m[32m  if (!readAt) return true;[m
[32m+[m[32m  return readAt.toMillis?.() < chat.lastMessageAt.toMillis?.();[m
[32m+[m[32m}[m
[32m+[m
 export default function ChatList() {[m
   const [chats, setChats] = useState([]);[m
   const [ready, setReady] = useState(false);[m
[32m+[m[32m  const [uid, setUid] = useState(null);[m
 [m
   useEffect(() => {[m
     ensureLoggedIn().then((user) => {[m
[32m+[m[32m      setUid(user.uid);[m
       const q = query([m
         collection(db, 'chats'),[m
         where('participants', 'array-contains', user.uid),[m
[36m@@ -23,20 +44,59 @@[m [mexport default function ChatList() {[m
     }).catch((err) => { console.error(err); setReady(true); });[m
   }, []);[m
 [m
[32m+[m[32m  const supportChatId = uid ? `support_${uid}` : null;[m
[32m+[m[32m  const supportChat = chats.find((c) => c.id === supportChatId);[m
[32m+[m[32m  const otherChats = chats.filter((c) => c.id !== supportChatId);[m
[32m+[m
   return ([m
     <div className="page">[m
       <h2 className="page-title">Messages</h2>[m
[31m-      {!ready && <p className="helper-text">Loading conversations...</p>}[m
[31m-      {ready && chats.length === 0 && <p className="helper-text">No conversations yet. Message a seller from a product page to start one.</p>}[m
[31m-      {chats.map((c) => ([m
[31m-        <Link to={`/chat/${c.id}`} className="chat-list-item" key={c.id}>[m
[31m-          <div className="chat-thumb"><Icon name="image" size={18} /></div>[m
[32m+[m
[32m+[m[32m      {uid && ([m
[32m+[m[32m        <Link to={`/chat/${supportChatId}`} className="chat-list-item support-pin">[m
[32m+[m[32m          <div className="chat-thumb support-thumb"><Icon name="helpCircle" size={20} /></div>[m
           <div className="chat-info">[m
[31m-            <div className="top"><span className="name">{c.listingTitle || 'Listing'}</span></div>[m
[31m-            <div className="msg">{c.lastMessage || 'No messages yet'}</div>[m
[32m+[m[32m            <div className="top">[m
[32m+[m[32m              <span className="name">{SUPPORT_NAME}</span>[m
[32m+[m[32m              {supportChat && <span className="time">{formatTime(supportChat.lastMessageAt)}</span>}[m
[32m+[m[32m            </div>[m
[32m+[m[32m            <div className="msg">[m
[32m+[m[32m              {supportChat?.lastMessage || "Need help? We're here for you."}[m
[32m+[m[32m            </div>[m
           </div>[m
[32m+[m[32m          {supportChat && isUnread(supportChat, uid) && <span className="unread-dot" />}[m
         </Link>[m
[31m-      ))}[m
[32m+[m[32m      )}[m
[32m+[m
[32m+[m[32m      {!ready && <p className="helper-text">Loading conversations...</p>}[m
[32m+[m[32m      {ready && otherChats.length === 0 && ([m
[32m+[m[32m        <p className="helper-text">No conversations yet. Message a seller from a product page to start one.</p>[m
[32m+[m[32m      )}[m
[32m+[m[32m      {otherChats.map((c) => {[m
[32m+[m[32m        const amBuyer = c.buyerId === uid;[m
[32m+[m[32m        const otherName = amBuyer ? (c.sellerName || 'Seller') : (c.buyerName || 'Buyer');[m
[32m+[m[32m        const lastIsMine = c.lastSenderId === uid;[m
[32m+[m[32m        return ([m
[32m+[m[32m          <Link to={`/chat/${c.id}`} className="chat-list-item" key={c.id}>[m
[32m+[m[32m            <div[m
[32m+[m[32m              className="chat-thumb"[m
[32m+[m[32m              style={c.listingPhoto ? { backgroundImage: `url(${c.listingPhoto})`, backgroundSize: 'cover', backgroundPosition: 'center' } : {}}[m
[32m+[m[32m            >[m
[32m+[m[32m              {!c.listingPhoto && <Icon name="image" size={18} />}[m
[32m+[m[32m            </div>[m
[32m+[m[32m            <div className="chat-info">[m
[32m+[m[32m              <div className="top">[m
[32m+[m[32m                <span className="name">{otherName}</span>[m
[32m+[m[32m                <span className="time">{formatTime(c.lastMessageAt)}</span>[m
[32m+[m[32m              </div>[m
[32m+[m[32m              <div className="msg">[m
[32m+[m[32m                {lastIsMine && c.lastMessage ? 'You: ' : ''}{c.lastMessage || c.listingTitle || 'No messages yet'}[m
[32m+[m[32m              </div>[m
[32m+[m[32m            </div>[m
[32m+[m[32m            {isUnread(c, uid) && <span className="unread-dot" />}[m
[32m+[m[32m          </Link>[m
[32m+[m[32m        );[m
[32m+[m[32m      })}[m
     </div>[m
   );[m
 }[m
[1mdiff --git a/frontend/src/pages/ChatThread.jsx b/frontend/src/pages/ChatThread.jsx[m
[1mindex e7722bc..760daea 100644[m
[1m--- a/frontend/src/pages/ChatThread.jsx[m
[1m+++ b/frontend/src/pages/ChatThread.jsx[m
[36m@@ -1,68 +1,232 @@[m
[31m-import React, { useEffect, useState } from 'react';[m
[32m+[m[32mimport React, { useEffect, useRef, useState } from 'react';[m
 import { Link, useParams } from 'react-router-dom';[m
[31m-import { collection, addDoc, doc, serverTimestamp, orderBy, query, onSnapshot, updateDoc } from 'firebase/firestore';[m
[31m-import { auth, db, ensureLoggedIn } from '../lib/firebase';[m
[32m+[m[32mimport {[m
[32m+[m[32m  collection, addDoc, doc, getDoc, setDoc, updateDoc,[m
[32m+[m[32m  serverTimestamp, orderBy, query, onSnapshot,[m
[32m+[m[32m} from 'firebase/firestore';[m
[32m+[m[32mimport { auth, db, ensureLoggedIn, notifyNewMessage } from '../lib/firebase';[m
[32m+[m[32mimport { getUnsafeUserPreview, getTelegramWebApp } from '../lib/telegram';[m
[32m+[m[32mimport { SUPPORT_UID, SUPPORT_NAME } from '../lib/constants';[m
 import Icon from '../components/Icon.jsx';[m
 [m
[32m+[m[32mfunction formatClock(ts) {[m
[32m+[m[32m  if (!ts?.toDate) return '';[m
[32m+[m[32m  return ts.toDate().toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });[m
[32m+[m[32m}[m
[32m+[m[32mfunction dayKey(ts) {[m
[32m+[m[32m  if (!ts?.toDate) return '';[m
[32m+[m[32m  return ts.toDate().toDateString();[m
[32m+[m[32m}[m
[32m+[m[32mfunction formatDaySeparator(ts) {[m
[32m+[m[32m  if (!ts?.toDate) return '';[m
[32m+[m[32m  const d = ts.toDate();[m
[32m+[m[32m  const now = new Date();[m
[32m+[m[32m  if (d.toDateString() === now.toDateString()) return 'Today';[m
[32m+[m[32m  const yesterday = new Date(now); yesterday.setDate(now.getDate() - 1);[m
[32m+[m[32m  if (d.toDateString() === yesterday.toDateString()) return 'Yesterday';[m
[32m+[m[32m  return d.toLocaleDateString([], { month: 'long', day: 'numeric', year: d.getFullYear() !== now.getFullYear() ? 'numeric' : undefined });[m
[32m+[m[32m}[m
[32m+[m
 export default function ChatThread() {[m
   const { id } = useParams();[m
[32m+[m[32m  const isSupport = id.startsWith('support_');[m
   const [messages, setMessages] = useState([]);[m
   const [text, setText] = useState('');[m
   const [chatInfo, setChatInfo] = useState(null);[m
[32m+[m[32m  const [error, setError] = useState('');[m
[32m+[m[32m  const [sending, setSending] = useState(false);[m
[32m+[m[32m  const bottomRef = useRef(null);[m
[32m+[m[32m  const textareaRef = useRef(null);[m
[32m+[m
[32m+[m[32m  // Keep the input row aligned with the on-screen keyboard inside the[m
[32m+[m[32m  // Telegram WebView (and as a fallback in a regular mobile browser),[m
[32m+[m[32m  // by tracking the live viewport height in a CSS variable.[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    const tg = getTelegramWebApp();[m
[32m+[m[32m    function updateViewport() {[m
[32m+[m[32m      const h = tg?.viewportStableHeight || window.visualViewport?.height || window.innerHeight;[m
[32m+[m[32m      document.documentElement.style.setProperty('--app-vh', `${h}px`);[m
[32m+[m[32m    }[m
[32m+[m[32m    updateViewport();[m
[32m+[m[32m    tg?.onEvent?.('viewportChanged', updateViewport);[m
[32m+[m[32m    window.visualViewport?.addEventListener('resize', updateViewport);[m
[32m+[m[32m    return () => {[m
[32m+[m[32m      tg?.offEvent?.('viewportChanged', updateViewport);[m
[32m+[m[32m      window.visualViewport?.removeEventListener('resize', updateViewport);[m
[32m+[m[32m    };[m
[32m+[m[32m  }, []);[m
 [m
   useEffect(() => {[m
[31m-    ensureLoggedIn().then(() => {[m
[31m-      const unsubChat = onSnapshot(doc(db, 'chats', id), (snap) => {[m
[32m+[m[32m    let unsubChat = () => {};[m
[32m+[m[32m    let unsubMsgs = () => {};[m
[32m+[m[32m    ensureLoggedIn().then(async (user) => {[m
[32m+[m[32m      // The support thread has a deterministic ID and is created[m
[32m+[m[32m      // lazily the first time the user opens it.[m
[32m+[m[32m      if (isSupport) {[m
[32m+[m[32m        const chatRef = doc(db, 'chats', id);[m
[32m+[m[32m        const snap = await getDoc(chatRef);[m
[32m+[m[32m        if (!snap.exists()) {[m
[32m+[m[32m          await setDoc(chatRef, {[m
[32m+[m[32m            participants: [SUPPORT_UID, user.uid],[m
[32m+[m[32m            buyerId: user.uid,[m
[32m+[m[32m            buyerName: getUnsafeUserPreview()?.first_name || 'You',[m
[32m+[m[32m            sellerId: SUPPORT_UID,[m
[32m+[m[32m            sellerName: SUPPORT_NAME,[m
[32m+[m[32m            isSupport: true,[m
[32m+[m[32m            listingTitle: '',[m
[32m+[m[32m            listingPhoto: '',[m
[32m+[m[32m            lastMessage: '',[m
[32m+[m[32m            lastSenderId: '',[m
[32m+[m[32m            createdAt: serverTimestamp(),[m
[32m+[m[32m            lastMessageAt: serverTimestamp(),[m
[32m+[m[32m          });[m
[32m+[m[32m        }[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      unsubChat = onSnapshot(doc(db, 'chats', id), (snap) => {[m
         if (snap.exists()) setChatInfo(snap.data());[m
       });[m
       const q = query(collection(db, 'chats', id, 'messages'), orderBy('createdAt', 'asc'));[m
[31m-      const unsubMsgs = onSnapshot(q, (snap) => setMessages(snap.docs.map((d) => ({ id: d.id, ...d.data() }))));[m
[31m-      return () => { unsubChat(); unsubMsgs(); };[m
[31m-    }).catch((err) => console.error(err));[m
[31m-  }, [id]);[m
[32m+[m[32m      unsubMsgs = onSnapshot(q, (snap) => setMessages(snap.docs.map((d) => ({ id: d.id, ...d.data() }))));[m
[32m+[m[32m    }).catch((err) => setError(err.message || "Couldn't open this chat."));[m
[32m+[m[32m    return () => { unsubChat(); unsubMsgs(); };[m
[32m+[m[32m  }, [id, isSupport]);[m
[32m+[m
[32m+[m[32m  // Auto-scroll to the newest message.[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    bottomRef.current?.scrollIntoView({ behavior: 'smooth', block: 'end' });[m
[32m+[m[32m  }, [messages.length]);[m
[32m+[m
[32m+[m[32m  // Mark the thread as read while it's open.[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    if (!messages.length || !auth.currentUser) return;[m
[32m+[m[32m    updateDoc(doc(db, 'chats', id), { [`lastReadAt.${auth.currentUser.uid}`]: serverTimestamp() }).catch(() => {});[m
[32m+[m[32m  }, [messages.length, id]);[m
[32m+[m
[32m+[m[32m  // Auto-grow the textarea up to a max height.[m
[32m+[m[32m  useEffect(() => {[m
[32m+[m[32m    const el = textareaRef.current;[m
[32m+[m[32m    if (!el) return;[m
[32m+[m[32m    el.style.height = 'auto';[m
[32m+[m[32m    el.style.height = `${Math.min(el.scrollHeight, 110)}px`;[m
[32m+[m[32m  }, [text]);[m
 [m
   async function send() {[m
[31m-    if (!text.trim()) return;[m
[32m+[m[32m    if (!text.trim() || sending) return;[m
[32m+[m[32m    setSending(true);[m
[32m+[m[32m    setError('');[m
[32m+[m[32m    const outgoing = text.trim();[m
     try {[m
       const user = await ensureLoggedIn();[m
       await addDoc(collection(db, 'chats', id, 'messages'), {[m
         senderId: user.uid,[m
[31m-        text,[m
[32m+[m[32m        text: outgoing,[m
         createdAt: serverTimestamp(),[m
       });[m
[31m-      await updateDoc(doc(db, 'chats', id), { lastMessage: text, lastMessageAt: serverTimestamp() });[m
[32m+[m[32m      await updateDoc(doc(db, 'chats', id), {[m
[32m+[m[32m        lastMessage: outgoing,[m
[32m+[m[32m        lastSenderId: user.uid,[m
[32m+[m[32m        lastMessageAt: serverTimestamp(),[m
[32m+[m[32m        [`lastReadAt.${user.uid}`]: serverTimestamp(),[m
[32m+[m[32m      });[m
       setText('');[m
[32m+[m
[32m+[m[32m      if (chatInfo) {[m
[32m+[m[32m        const recipientUid = chatInfo.buyerId === user.uid ? chatInfo.sellerId : chatInfo.buyerId;[m
[32m+[m[32m        notifyNewMessage({[m
[32m+[m[32m          recipientUid,[m
[32m+[m[32m          senderName: getUnsafeUserPreview()?.first_name || 'Someone',[m
[32m+[m[32m          listingTitle: chatInfo.listingTitle,[m
[32m+[m[32m          text: outgoing,[m
[32m+[m[32m          chatId: id,[m
[32m+[m[32m        });[m
[32m+[m[32m      }[m
     } catch (err) {[m
[31m-      alert(`Couldn't send: ${err.message || err}`);[m
[32m+[m[32m      setError(err.message || "Couldn't send that message.");[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      setSending(false);[m
     }[m
   }[m
 [m
[32m+[m[32m  function onKeyDown(e) {[m
[32m+[m[32m    if (e.key === 'Enter' && !e.shiftKey) {[m
[32m+[m[32m      e.preventDefault();[m
[32m+[m[32m      send();[m
[32m+[m[32m    }[m
[32m+[m[32m  }[m
[32m+[m
[32m+[m[32m  const myUid = auth.currentUser?.uid;[m
[32m+[m[32m  const amBuyer = chatInfo?.buyerId === myUid;[m
[32m+[m[32m  const otherName = chatInfo?.isSupport[m
[32m+[m[32m    ? SUPPORT_NAME[m
[32m+[m[32m    : (amBuyer ? (chatInfo?.sellerName || 'Seller') : (chatInfo?.buyerName || 'Buyer'));[m
[32m+[m
   return ([m
     <div className="page thread-page">[m
       <div className="chat-context">[m
         <Link to="/chat" className="chat-back" aria-label="Back to messages">[m
           <Icon name="chevronLeft" size={19} />[m
         </Link>[m
[31m-        <div className="thumb-mini" />[m
[32m+[m[32m        <div className={`thumb-mini ${chatInfo?.isSupport ? 'support-thumb' : ''}`}>[m
[32m+[m[32m          {chatInfo?.isSupport && <Icon name="helpCircle" size={16} />}[m
[32m+[m[32m        </div>[m
         <div className="info">[m
[31m-          <div className="t">{chatInfo?.listingTitle || 'Listing'}</div>[m
[32m+[m[32m          <div className="n">{otherName}</div>[m
[32m+[m[32m          {!chatInfo?.isSupport && chatInfo?.listingTitle && <div className="t">{chatInfo.listingTitle}</div>}[m
         </div>[m
         {chatInfo?.listingId && ([m
           <Link to={`/product/${chatInfo.listingId}`} className="p" style={{ textDecoration: 'none' }}>View</Link>[m
         )}[m
       </div>[m
[31m-      <div className="safety-banner">[m
[31m-        <Icon name="shield" size={16} />[m
[31m-        <span>Meet the seller in person and inspect the item before you pay. Never send money in advance.</span>[m
[31m-      </div>[m
[32m+[m
[32m+[m[32m      {!chatInfo?.isSupport && ([m
[32m+[m[32m        <div className="safety-banner">[m
[32m+[m[32m          <Icon name="shield" size={16} />[m
[32m+[m[32m          <span>Meet the seller in person and inspect the item before you pay. Never send money in advance.</span>[m
[32m+[m[32m        </div>[m
[32m+[m[32m      )}[m
[32m+[m
       <div className="thread">[m
[31m-        {messages.map((m) => ([m
[31m-          <div key={m.id} className={`bubble ${m.senderId === auth.currentUser?.uid ? 'me' : 'them'}`}>{m.text}</div>[m
[31m-        ))}[m
[32m+[m[32m        {messages.map((m, i) => {[m
[32m+[m[32m          const prev = messages[i - 1];[m
[32m+[m[32m          const next = messages[i + 1];[m
[32m+[m[32m          const newDay = !prev || dayKey(m.createdAt) !== dayKey(prev.createdAt);[m
[32m+[m[32m          const firstInGroup = newDay || !prev || prev.senderId !== m.senderId;[m
[32m+[m[32m          const lastInGroup = !next || next.senderId !== m.senderId || dayKey(next.createdAt) !== dayKey(m.createdAt);[m
[32m+[m[32m          const mine = m.senderId === myUid;[m
[32m+[m[32m          return ([m
[32m+[m[32m            <React.Fragment key={m.id}>[m
[32m+[m[32m              {newDay && <div className="date-sep"><span>{formatDaySeparator(m.createdAt)}</span></div>}[m
[32m+[m[32m              <div className={`bubble ${mine ? 'me' : 'them'} ${firstInGroup ? 'first' : 'grouped'}`}>[m
[32m+[m[32m                {m.text}[m
[32m+[m[32m              </div>[m
[32m+[m[32m              {lastInGroup && <div className={`bubble-time ${mine ? 'me' : 'them'}`}>{formatClock(m.createdAt)}</div>}[m
[32m+[m[32m            </React.Fragment>[m
[32m+[m[32m          );[m
[32m+[m[32m        })}[m
[32m+[m[32m        <div ref={bottomRef} />[m
       </div>[m
[32m+[m
[32m+[m[32m      {error && ([m
[32m+[m[32m        <div className="error-banner">[m
[32m+[m[32m          <Icon name="x" size={14} />[m
[32m+[m[32m          <span>{error}</span>[m
[32m+[m[32m        </div>[m
[32m+[m[32m      )}[m
[32m+[m
       <div className="chat-input-row">[m
[31m-        <input className="field" value={text} onChange={(e) => setText(e.target.value)} placeholder="Type a message..." onKeyDown={(e) => e.key === 'Enter' && send()} />[m
[31m-        <button className="send-btn" onClick={send}><Icon name="send" size={17} /></button>[m
[32m+[m[32m        <textarea[m
[32m+[m[32m          ref={textareaRef}[m
[32m+[m[32m          className="field"[m
[32m+[m[32m          rows={1}[m
[32m+[m[32m          value={text}[m
[32m+[m[32m          onChange={(e) => setText(e.target.value)}[m
[32m+[m[32m          onKeyDown={onKeyDown}[m
[32m+[m[32m          placeholder="Type a message..."[m
[32m+[m[32m        />[m
[32m+[m[32m        <button className="send-btn" onClick={send} disabled={sending || !text.trim()}>[m
[32m+[m[32m          <Icon name="send" size={17} />[m
[32m+[m[32m        </button>[m
       </div>[m
     </div>[m
   );[m
[1mdiff --git a/frontend/src/pages/Home.jsx b/frontend/src/pages/Home.jsx[m
[1mindex 20facd2..a9a813d 100644[m
[1m--- a/frontend/src/pages/Home.jsx[m
[1m+++ b/frontend/src/pages/Home.jsx[m
[36m@@ -1,9 +1,12 @@[m
[31m-import React, { useEffect, useState } from 'react';[m
[32m+[m[32mimport React, { useEffect, useMemo, useState } from 'react';[m
 import { Link } from 'react-router-dom';[m
 import { collection, query, orderBy, limit, getDocs } from 'firebase/firestore';[m
 import { db } from '../lib/firebase';[m
 import { CATEGORIES } from '../data/categories';[m
 import Icon from '../components/Icon.jsx';[m
[32m+[m[32mimport SearchHeader from '../components/SearchHeader.jsx';[m
[32m+[m[32mimport FilterSheet from '../components/FilterSheet.jsx';[m
[32m+[m[32mimport EmptyState from '../components/EmptyState.jsx';[m
 [m
 const SWATCHES = ['#8FA998', '#C9A15A', '#A9876B', '#8A9BAE', '#B0836D', '#7E9E8C', '#B79A6B', '#93A0AE'];[m
 function colorFor(id) {[m
[36m@@ -12,15 +15,17 @@[m [mfunction colorFor(id) {[m
   return SWATCHES[h];[m
 }[m
 [m
[32m+[m[32mconst EMPTY_FILTERS = { minPrice: null, maxPrice: null, conditions: [] };[m
[32m+[m
 export default function Home() {[m
   const [listings, setListings] = useState([]);[m
   const [loading, setLoading] = useState(true);[m
   const [search, setSearch] = useState('');[m
   const [activeCategory, setActiveCategory] = useState(null);[m
[31m-  const [sortBy, setSortBy] = useState('newest'); // 'newest' | 'price-asc'[m
[32m+[m[32m  const [filters, setFilters] = useState(EMPTY_FILTERS);[m
[32m+[m[32m  const [filterSheetOpen, setFilterSheetOpen] = useState(false);[m
 [m
   useEffect(() => {[m
[31m-    // Public read — no auth required. See firestore.rules.[m
     const q = query(collection(db, 'listings'), orderBy('createdAt', 'desc'), limit(30));[m
     getDocs(q)[m
       .then((snap) => setListings(snap.docs.map((d) => ({ id: d.id, ...d.data() }))))[m
[36m@@ -30,60 +35,76 @@[m [mexport default function Home() {[m
   const boosted = listings.filter((l) => l.boostedUntil && l.boostedUntil.toDate?.() > new Date());[m
 [m
   const term = search.trim().toLowerCase();[m
[31m-  let filtered = listings.filter((l) => {[m
[31m-    const matchesTerm = !term || l.title?.toLowerCase().includes(term);[m
[31m-    const matchesCategory = !activeCategory || l.category === activeCategory;[m
[31m-    return matchesTerm && matchesCategory;[m
[31m-  });[m
[31m-  if (sortBy === 'price-asc') {[m
[31m-    filtered = [...filtered].sort((a, b) => (a.price || 0) - (b.price || 0));[m
[31m-  }[m
 [m
[31m-  const filtering = term || activeCategory;[m
[32m+[m[32m  const filtered = useMemo(() => {[m
[32m+[m[32m    return listings.filter((l) => {[m
[32m+[m[32m      const matchesTerm = !term || l.title?.toLowerCase().includes(term);[m
[32m+[m[32m      const matchesCategory = !activeCategory || l.category === activeCategory;[m
[32m+[m[32m      const matchesMin = filters.minPrice == null || (l.price || 0) >= filters.minPrice;[m
[32m+[m[32m      const matchesMax = filters.maxPrice == null || (l.price || 0) <= filters.maxPrice;[m
[32m+[m[32m      const matchesCondition = filters.conditions.length === 0 || filters.conditions.includes(l.condition);[m
[32m+[m[32m      return matchesTerm && matchesCategory && matchesMin && matchesMax && matchesCondition;[m
[32m+[m[32m    });[m
[32m+[m[32m  }, [listings, term, activeCategory, filters]);[m
[32m+[m
[32m+[m[32m  const suggestions = useMemo(() => {[m
[32m+[m[32m    if (!term) return [];[m
[32m+[m[32m    const seen = new Set();[m
[32m+[m[32m    const out = [];[m
[32m+[m[32m    for (const l of listings) {[m
[32m+[m[32m      const title = l.title?.trim();[m
[32m+[m[32m      if (title && title.toLowerCase().includes(term) && !seen.has(title)) {[m
[32m+[m[32m        seen.add(title);[m
[32m+[m[32m        out.push(title);[m
[32m+[m[32m      }[m
[32m+[m[32m    }[m
[32m+[m[32m    return out;[m
[32m+[m[32m  }, [listings, term]);[m
[32m+[m
[32m+[m[32m  const popularTags = useMemo(() => {[m
[32m+[m[32m    const seen = new Set();[m
[32m+[m[32m    const out = [];[m
[32m+[m[32m    for (const l of [...boosted, ...listings]) {[m
[32m+[m[32m      const title = l.title?.trim();[m
[32m+[m[32m      if (title && !seen.has(title)) {[m
[32m+[m[32m        seen.add(title);[m
[32m+[m[32m        out.push(title);[m
[32m+[m[32m      }[m
[32m+[m[32m      if (out.length >= 6) break;[m
[32m+[m[32m    }[m
[32m+[m[32m    return out;[m
[32m+[m[32m  }, [listings, boosted]);[m
[32m+[m
[32m+[m[32m  const filtering = Boolean(term || activeCategory || filters.minPrice != null || filters.maxPrice != null || filters.conditions.length > 0);[m
[32m+[m[32m  const activeFilterCount = (filters.minPrice != null ? 1 : 0) + (filters.maxPrice != null ? 1 : 0) + (filters.conditions.length > 0 ? 1 : 0);[m
[32m+[m
[32m+[m[32m  function clearAll() {[m
[32m+[m[32m    setSearch('');[m
[32m+[m[32m    setActiveCategory(null);[m
[32m+[m[32m    setFilters(EMPTY_FILTERS);[m
[32m+[m[32m  }[m
 [m
   return ([m
     <div className="page">[m
[31m-      <div className="search-bar">[m
[31m-        <Icon name="search" size={17} style={{ color: 'var(--ink-faint)' }} />[m
[31m-        <input[m
[31m-          placeholder="Search — e.g. iPhone, sofa, Vitz..."[m
[31m-          value={search}[m
[31m-          onChange={(e) => setSearch(e.target.value)}[m
[31m-        />[m
[31m-        {search && ([m
[31m-          <Icon name="x" size={16} style={{ color: 'var(--ink-faint)', cursor: 'pointer' }} onClick={() => setSearch('')} />[m
[31m-        )}[m
[31m-      </div>[m
[32m+[m[32m      <SearchHeader[m
[32m+[m[32m        value={search}[m
[32m+[m[32m        onChange={setSearch}[m
[32m+[m[32m        onSubmit={() => {}}[m
[32m+[m[32m        suggestions={suggestions}[m
[32m+[m[32m        popularTags={popularTags}[m
[32m+[m[32m        categories={CATEGORIES}[m
[32m+[m[32m        activeCategory={activeCategory}[m
[32m+[m[32m        onCategoryChange={setActiveCategory}[m
[32m+[m[32m        onOpenFilters={() => setFilterSheetOpen(true)}[m
[32m+[m[32m        activeFilterCount={activeFilterCount}[m
[32m+[m[32m      />[m
 [m
[31m-      <div className="chip-row">[m
[31m-        <div className="chip active"><Icon name="mapPin" size={14} /> Holeta</div>[m
[31m-        <div[m
[31m-          className={`chip ${sortBy === 'price-asc' ? 'active' : ''}`}[m
[31m-          onClick={() => setSortBy(sortBy === 'price-asc' ? 'newest' : 'price-asc')}[m
[31m-        >[m
[31m-          {sortBy === 'price-asc' ? 'Price: Low to High' : 'All prices'}[m
[31m-        </div>[m
[31m-        <div className="chip" onClick={() => setSortBy('newest')}>Newest</div>[m
[31m-        {activeCategory && ([m
[31m-          <div className="chip active" onClick={() => setActiveCategory(null)}>[m
[31m-            {CATEGORIES.find((c) => c.id === activeCategory)?.name} <Icon name="x" size={12} />[m
[31m-          </div>[m
[31m-        )}[m
[31m-      </div>[m
[31m-[m
[31m-      <h3 className="section-title">Categories</h3>[m
[31m-      <div className="cat-grid">[m
[31m-        {CATEGORIES.map((c) => ([m
[31m-          <div[m
[31m-            className={`cat-item ${activeCategory === c.id ? 'active' : ''}`}[m
[31m-            key={c.id}[m
[31m-            onClick={() => setActiveCategory(activeCategory === c.id ? null : c.id)}[m
[31m-          >[m
[31m-            <div className="cat-icon"><Icon name={c.icon || 'grid'} size={19} /></div>[m
[31m-            <span>{c.name}</span>[m
[31m-          </div>[m
[31m-        ))}[m
[31m-      </div>[m
[32m+[m[32m      <FilterSheet[m
[32m+[m[32m        open={filterSheetOpen}[m
[32m+[m[32m        onClose={() => setFilterSheetOpen(false)}[m
[32m+[m[32m        filters={filters}[m
[32m+[m[32m        onApply={setFilters}[m
[32m+[m[32m      />[m
 [m
       {!filtering && boosted.length > 0 && ([m
         <>[m
[36m@@ -96,11 +117,20 @@[m [mexport default function Home() {[m
 [m
       <h3 className="section-title">{filtering ? 'Results' : 'Recent Listings'}</h3>[m
       {loading && <p className="helper-text">Loading...</p>}[m
[32m+[m
       {!loading && filtered.length === 0 && ([m
[31m-        <p className="helper-text">[m
[31m-          {filtering ? 'No listings match your search or filter.' : 'No listings yet — be the first to post one.'}[m
[31m-        </p>[m
[32m+[m[32m        filtering ? ([m
[32m+[m[32m          <EmptyState[m
[32m+[m[32m            term={search.trim()}[m
[32m+[m[32m            suggestions={popularTags.slice(0, 4)}[m
[32m+[m[32m            onSuggestionClick={(s) => setSearch(s)}[m
[32m+[m[32m            onClear={clearAll}[m
[32m+[m[32m          />[m
[32m+[m[32m        ) : ([m
[32m+[m[32m          <p className="helper-text">No listings yet — be the first to post one.</p>[m
[32m+[m[32m        )[m
       )}[m
[32m+[m
       <div className="listing-grid">[m
         {filtered.map((item) => <ListingCard key={item.id} item={item} />)}[m
       </div>[m
[1mdiff --git a/frontend/src/pages/PostAd.jsx b/frontend/src/pages/PostAd.jsx[m
[1mindex 4cc629c..46efa66 100644[m
[1m--- a/frontend/src/pages/PostAd.jsx[m
[1m+++ b/frontend/src/pages/PostAd.jsx[m
[36m@@ -2,6 +2,7 @@[m [mimport React, { useState } from 'react';[m
 import { useNavigate } from 'react-router-dom';[m
 import { collection, addDoc, serverTimestamp } from 'firebase/firestore';[m
 import { db, ensureLoggedIn } from '../lib/firebase';[m
[32m+[m[32mimport { getUnsafeUserPreview } from '../lib/telegram';[m
 import { fileToCompressedBase64 } from '../lib/imageCompress';[m
 import { CATEGORIES, getSubcategory } from '../data/categories';[m
 import DynamicAttributeForm from '../components/DynamicAttributeForm.jsx';[m
[36m@@ -60,8 +61,15 @@[m [mexport default function PostAd() {[m
       }[m
 [m
       setStatusMsg('Publishing...');[m
[32m+[m[32m      // Display name is taken from Telegram's own client-side preview[m
[32m+[m[32m      // (not a Firestore read of another user's doc — that's blocked[m
[32m+[m[32m      // by firestore.rules) and stored on the listing so buyers can[m
[32m+[m[32m      // see who they'd be chatting with without any extra reads.[m
[32m+[m[32m      const sellerName = getUnsafeUserPreview()?.first_name || 'Seller';[m
[32m+[m
       const ref2 = await addDoc(collection(db, 'listings'), {[m
         sellerId: user.uid,[m
[32m+[m[32m        sellerName,[m
         title,[m
         price: Number(price),[m
         description,[m
[1mdiff --git a/frontend/src/pages/ProductDetail.jsx b/frontend/src/pages/ProductDetail.jsx[m
[1mindex 12b7e41..1c20de0 100644[m
[1m--- a/frontend/src/pages/ProductDetail.jsx[m
[1m+++ b/frontend/src/pages/ProductDetail.jsx[m
[36m@@ -1,13 +1,16 @@[m
 import React, { useEffect, useState } from 'react';[m
 import { useParams, useNavigate } from 'react-router-dom';[m
[31m-import { doc, getDoc, collection, addDoc, serverTimestamp } from 'firebase/firestore';[m
[32m+[m[32mimport { doc, getDoc, collection, addDoc, query, where, getDocs, limit, serverTimestamp } from 'firebase/firestore';[m
 import { db, ensureLoggedIn } from '../lib/firebase';[m
[32m+[m[32mimport { getUnsafeUserPreview } from '../lib/telegram';[m
 import Icon from '../components/Icon.jsx';[m
 [m
 export default function ProductDetail() {[m
   const { id } = useParams();[m
   const navigate = useNavigate();[m
   const [item, setItem] = useState(null);[m
[32m+[m[32m  const [chatError, setChatError] = useState('');[m
[32m+[m[32m  const [startingChat, setStartingChat] = useState(false);[m
 [m
   useEffect(() => {[m
     getDoc(doc(db, 'listings', id)).then((snap) => {[m
[36m@@ -18,20 +21,45 @@[m [mexport default function ProductDetail() {[m
   // Auth is only requested HERE — the moment the user wants to act[m
   // (message the seller), never before this point.[m
   async function startChat() {[m
[32m+[m[32m    if (startingChat) return;[m
[32m+[m[32m    setChatError('');[m
[32m+[m[32m    setStartingChat(true);[m
     try {[m
       const user = await ensureLoggedIn();[m
[32m+[m
[32m+[m[32m      // Reuse an existing thread for this listing+buyer instead of[m
[32m+[m[32m      // creating a duplicate every time "Chat with Seller" is tapped.[m
[32m+[m[32m      const existing = await getDocs(query([m
[32m+[m[32m        collection(db, 'chats'),[m
[32m+[m[32m        where('listingId', '==', id),[m
[32m+[m[32m        where('buyerId', '==', user.uid),[m
[32m+[m[32m        limit(1)[m
[32m+[m[32m      ));[m
[32m+[m[32m      if (!existing.empty) {[m
[32m+[m[32m        navigate(`/chat/${existing.docs[0].id}`);[m
[32m+[m[32m        return;[m
[32m+[m[32m      }[m
[32m+[m
[32m+[m[32m      const buyerName = getUnsafeUserPreview()?.first_name || 'Buyer';[m
       const chatRef = await addDoc(collection(db, 'chats'), {[m
         listingId: id,[m
         listingTitle: item.title,[m
[32m+[m[32m        listingPhoto: item.images?.[0] || '',[m
         sellerId: item.sellerId,[m
[32m+[m[32m        sellerName: item.sellerName || 'Seller',[m
         buyerId: user.uid,[m
[32m+[m[32m        buyerName,[m
         participants: [item.sellerId, user.uid],[m
[32m+[m[32m        lastMessage: '',[m
[32m+[m[32m        lastSenderId: '',[m
         createdAt: serverTimestamp(),[m
         lastMessageAt: serverTimestamp(),[m
       });[m
       navigate(`/chat/${chatRef.id}`);[m
     } catch (err) {[m
[31m-      alert(`Couldn't start chat: ${err.message || err}`);[m
[32m+[m[32m      setChatError(err.message || "Couldn't start chat. Please try again.");[m
[32m+[m[32m    } finally {[m
[32m+[m[32m      setStartingChat(false);[m
     }[m
   }[m
 [m
[36m@@ -70,13 +98,20 @@[m [mexport default function ProductDetail() {[m
           <Icon name="shield" size={16} />[m
           <span>Meet the seller in person and inspect the item before you pay. Never send money in advance.</span>[m
         </div>[m
[32m+[m
[32m+[m[32m        {chatError && ([m
[32m+[m[32m          <div className="error-banner">[m
[32m+[m[32m            <Icon name="x" size={14} />[m
[32m+[m[32m            <span>{chatError}</span>[m
[32m+[m[32m          </div>[m
[32m+[m[32m        )}[m
       </div>[m
       <div className="sticky-actions">[m
[31m-        <button className="btn btn-outline-primary" onClick={() => ensureLoggedIn().then(() => alert('Calling — number revealed after login.')).catch((e) => alert(e.message))}>[m
[32m+[m[32m        <button className="btn btn-outline-primary" onClick={() => ensureLoggedIn().then(() => alert('Calling — number revealed after login.')).catch((e) => setChatError(e.message))}>[m
           <Icon name="phone" size={16} /> Call[m
         </button>[m
[31m-        <button className="btn btn-primary" onClick={startChat}>[m
[31m-          <Icon name="chat" size={16} /> Chat with Seller[m
[32m+[m[32m        <button className="btn btn-primary" onClick={startChat} disabled={startingChat}>[m
[32m+[m[32m          <Icon name="chat" size={16} /> {startingChat ? 'Opening…' : 'Chat with Seller'}[m
         </button>[m
       </div>[m
     </div>[m
[1mdiff --git a/frontend/src/styles/theme.css b/frontend/src/styles/theme.css[m
[1mindex 91a6cf6..3823bca 100644[m
[1m--- a/frontend/src/styles/theme.css[m
[1m+++ b/frontend/src/styles/theme.css[m
[36m@@ -88,29 +88,43 @@[m [mtextarea.field{min-height:84px;resize:vertical;}[m
 .upload-slot.filled{border-style:solid;}[m
 .upload-slot .x{position:absolute;top:4px;right:4px;width:20px;height:20px;background:rgba(0,0,0,.5);color:#fff;border-radius:50%;display:flex;align-items:center;justify-content:center;font-size:13px;line-height:1;}[m
 [m
[31m-.chat-list-item{display:flex;align-items:center;gap:11px;text-decoration:none;color:inherit;padding:12px 0;border-bottom:1px solid var(--line);}[m
[32m+[m[32m.chat-list-item{position:relative;display:flex;align-items:center;gap:11px;text-decoration:none;color:inherit;padding:12px 0;border-bottom:1px solid var(--line);}[m
 .chat-thumb{width:50px;height:50px;border-radius:12px;flex:0 0 auto;background:var(--surface-sunken);display:flex;align-items:center;justify-content:center;color:var(--ink-faint);}[m
[32m+[m[32m.chat-thumb.support-thumb,.thumb-mini.support-thumb{background:var(--primary-tint);color:var(--primary);display:flex;align-items:center;justify-content:center;}[m
[32m+[m[32m.chat-list-item.support-pin{margin-bottom:2px;}[m
 .chat-info{flex:1;min-width:0;}[m
 .chat-info .top{display:flex;justify-content:space-between;gap:8px;}[m
 .chat-info .name{font-weight:700;font-size:13.5px;}[m
 .chat-info .time{font-size:10.5px;color:var(--ink-faint);flex:0 0 auto;}[m
 .chat-info .msg{font-size:12px;color:var(--ink-soft);margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}[m
[32m+[m[32m.unread-dot{position:absolute;right:0;top:16px;width:9px;height:9px;border-radius:50%;background:var(--primary);flex:0 0 auto;}[m
 [m
 .chat-context{display:flex;align-items:center;gap:10px;background:var(--surface-sunken);margin:0 0 10px;padding:9px 10px;border-radius:12px;}[m
 .chat-back{display:flex;align-items:center;justify-content:center;width:28px;height:28px;flex:0 0 auto;color:var(--ink-soft);text-decoration:none;}[m
 .chat-context .thumb-mini{width:36px;height:36px;border-radius:9px;background:var(--surface);flex:0 0 auto;}[m
[31m-.chat-context .info{flex:1;}[m
[31m-.chat-context .t{font-size:12px;font-weight:700;}[m
[32m+[m[32m.chat-context .info{flex:1;min-width:0;}[m
[32m+[m[32m.chat-context .n{font-size:12.5px;font-weight:700;}[m
[32m+[m[32m.chat-context .t{font-size:11px;color:var(--ink-faint);margin-top:1px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}[m
 .chat-context .p{font-size:12px;font-weight:800;color:var(--primary);}[m
 [m
[31m-.thread-page{display:flex;flex-direction:column;min-height:100%;padding-bottom:0;}[m
[31m-.thread{display:flex;flex-direction:column;gap:8px;flex:1;padding:6px 0;}[m
[31m-.bubble{max-width:75%;padding:10px 13px;border-radius:16px;font-size:13px;line-height:1.5;}[m
[32m+[m[32m.thread-page{display:flex;flex-direction:column;min-height:calc(var(--app-vh, 100vh) - 0px);padding-bottom:0;}[m
[32m+[m[32m.thread{display:flex;flex-direction:column;gap:2px;flex:1;padding:6px 0;}[m
[32m+[m[32m.date-sep{display:flex;justify-content:center;margin:14px 0 8px;}[m
[32m+[m[32m.date-sep span{font-size:11px;font-weight:600;color:var(--ink-faint);background:var(--surface-sunken);padding:4px 11px;border-radius:20px;}[m
[32m+[m[32m.bubble{max-width:75%;padding:10px 13px;border-radius:16px;font-size:13px;line-height:1.5;white-space:pre-wrap;word-break:break-word;}[m
[32m+[m[32m.bubble.first{margin-top:8px;}[m
[32m+[m[32m.bubble.grouped{margin-top:2px;}[m
 .bubble.them{background:var(--surface);border:1px solid var(--line);align-self:flex-start;border-bottom-left-radius:5px;}[m
 .bubble.me{background:var(--primary);color:#fff;align-self:flex-end;border-bottom-right-radius:5px;}[m
[31m-.chat-input-row{display:flex;align-items:center;gap:9px;padding:10px 0 14px;position:sticky;bottom:0;background:var(--bg-app);}[m
[31m-.chat-input-row .field{flex:1;}[m
[32m+[m[32m.bubble-time{font-size:10px;color:var(--ink-faint);margin:2px 2px 6px;}[m
[32m+[m[32m.bubble-time.me{align-self:flex-end;}[m
[32m+[m[32m.bubble-time.them{align-self:flex-start;}[m
[32m+[m[32m.chat-input-row{display:flex;align-items:flex-end;gap:9px;padding:10px 0 14px;position:sticky;bottom:0;background:var(--bg-app);}[m
[32m+[m[32m.chat-input-row .field{flex:1;resize:none;max-height:110px;line-height:1.4;}[m
 .send-btn{background:var(--primary);color:#fff;border:none;border-radius:50%;width:42px;height:42px;flex:0 0 auto;display:flex;align-items:center;justify-content:center;cursor:pointer;}[m
[32m+[m[32m.send-btn:disabled{opacity:.45;cursor:default;}[m
[32m+[m
[32m+[m[32m.error-banner{display:flex;gap:8px;align-items:center;background:#FDECEA;color:#9B2C1F;border:1px solid #F3C4BC;border-radius:var(--radius-md);padding:10px 12px;font-size:12px;font-weight:500;margin:8px 0;}[m
 [m
 .stat-row{display:flex;gap:10px;}[m
 .stat-card{flex:1;background:var(--surface);border:1px solid var(--line);border-radius:var(--radius-md);padding:13px;box-shadow:var(--shadow-card);}[m
